# Project Notes

## Confirmed intent

- `wqb-cli 0.4.0` 的发布范围固定为 command-plugin SDK、持久化 SQLite 批量回测、模板集校验与报告、认证恢复，以及相互隔离的 `workflow_simu` / `workflow_batchsimu` A-M 文档；发布前必须通过完整测试、构建、wheel 安装冒烟和包内容审计。
- 0.4.0 只发布源码、随包文档和构建产物；不得包含 `local/`、`research_runs/`、SQLite、cookie、凭证、worker log 或历史回测结果。
- 在 `wqb_cli` 仓库内实现可接入 workflow 的批量回测能力。
- 逻辑优先参考旧回测架构的 `send_simulate_request.py`、`get_simulate_result.py`、`get_is_pnl.py`；如果语义或行为发生变化，必须明确说明。
- agent 只负责批量生成或接收入库表达式并启动 `wqb sqlitesimu`，不参与逐条发起回测、轮询和结果抓取。
- 回测完成后，基于持久化的 `simued_alpha_is_pnl` 数据做批量分析和后续优化。
- 当前 campaign 在独立 `workflow_batchsimu` 内完成 A-M，累计目标是平台确认成功提交 4 个 Alpha；simulate 与 Alpha submit 必须严格区分。
- 旧 2400 条 template-v2 已停止；先完成流程、检测语义与报告实现，再从本流程 A 创建新的 forum-v3 独立 batch。
- `workflow_batchsimu` 建议降低 `CHN` 和 `USA` 的选区优先级：前者当前要求 `Sharpe > 2.07`，后者参与者多、研究拥挤。该意见是研究建议而非硬禁令；B/C 有充分实时证据时仍可选择，`template-validate` 不得按 region 拒绝 manifest。
- 不使用 skill。
- `sqlitesimu` 不设置客户端显式并发槽位上限；持续向 BRAIN 发起 simulate 请求，由服务器 `429 / Retry-After` 控制背压，且 `429` 不消耗失败预算。
- 旧三脚本的删除语义应通过独立运行队列实现：simulation 结果确定后删除表达式待办，alpha 详情与 PnL 完整落库后删除 enrichment 待办；candidate、experiment、batch、Location、错误和结果历史不得随待办删除。
- `SIMULATE_UNKNOWN` 可能已经在服务器创建 simulation，必须保留 simulation 待办供人工核对，禁止自动删除或盲目重跑。
- `sqlitesimu` 单 worker 必须优先消费已到期的 parent、child、alpha detail/PnL 轮询，再继续创建新 batch 并发起回测，以复刻旧三脚本并行推进而不让大批入队造成轮询饥饿。
- enrichment 要优先完成已进入 `ENRICH_PNL` 的 alpha，使 detail/PnL 逐条闭环为 `READY` 并及时删除 enrichment 待办。
- 用户询问“现在进度”时，除 run/worker/queue 状态外，必须统计当时全部 `READY` 结果：核心指标分位数、BRAIN checks 通过/失败情况、主要失败原因，以及必要的模板族和最佳个体摘要；不能把 READY 数量等同于有效 alpha 数量。

## Release 0.4.0 verification (2026-08-18)

- Full WQBRAIN test suite: `95/95` passed; `git diff --check` passed.
- Final wheel and sdist were built from package version `0.4.0`; a clean venv installed the wheel with declared dependencies and `pip check` reported no broken requirements.
- Wheel smoke verified `wqb`, `wqb sqlitesimu`, `template-validate`, `template-report`, bundled API inventory, and both packaged A-M workflow trees from isolated `site-packages`.
- Artifact scan found no `local/`, `research_runs/`, SQLite, credential, cookie, `.env`, or log payloads.
- Wheel SHA-256: `c18ca5106927cfbd8438e1871cc48aac9151cfb4aa1f6c06c72cace257c704b1`.
- Sdist SHA-256: `ea70a2184bb0f372c200db25b3534b19c4f365b938a7bef35e9de99d7bde03af`.
- Setuptools reports the existing file-based custom license metadata as deprecated for 2027; the 0.4.0 artifacts still contain the full license text and `License-File`, so the expression was not rewritten during this release.

## Workflow separation

- `workflows/workflow_simu` 与 `workflows/workflow_batchsimu` 是两套完全独立的 workflow，不共享节点、A-F 产物、run 目录、SQLite 数据库或控制流 handoff。
- `workflow_simu` 自包含 A-M：少量候选、agent 在环、同步 `wqb sim create`、个体 K/L 诊断，M 才允许记录或执行提交。
- `workflow_batchsimu` 自包含 A-M：模板族密度研究、固定 settings、分族无放回抽样；J 只负责 `wqb sqlitesimu` 入库、启动 worker 和交接，K 分析并选择，L 慢速终检，M 才允许 Alpha submit。
- batch worker 运行期间 agent 不参与逐条回测；K 只能在权威 run 终态后分析 execution、错误、质量分布和 `simued_alpha_is_pnl` correlation clusters；L/M 只能处理当前 batch 自己的真实 Alpha。
- batch 不得向另一套 workflow 移交或接收 candidate、alpha、结果或控制流；累计目标未完成时必须从本流程 A 创建新的独立 batch。
- 新 run 分别放在 `research_runs/workflow_simu/` 与 `research_runs/workflow_batchsimu/`；2026-08-18 的历史 batch 保持原路径和数据库，不迁移，但已按用户要求停止并持久化取消状态。
- batch template 必须遵循 `workflows/workflow_batchsimu/template_contract.md`：H 使用中英文名、逻辑、placeholder 和 `template_LLM` 源码格式，I 生成完整 version/epoch/hash lineage，K 固定输出三段机器统计和逐模板评估。
- `expression_hash` 标识含 header 的完整源码，`calculation_hash` 标识可执行正文；跨 epoch/run 必须用 `calculation_hash + settings_hash` 排除已经测试过的等价计算。
- 固定模板报告只做格式化和基础完整性筛查，不能替代 F 的 denominator/Wilson contract 或 K 的 IS-PnL clustering，也不能单独授权候选进入 L。
- `SIMULATE_UNKNOWN` 逐 experiment 隔离且永远不自动重跑、不选择、不提交；仅由 unknown 导致的 `BLOCKED` run 在 READY coverage 达到 F 预注册门槛时，可以分析 READY 子集。
- `SELF_CORRELATION`、`DATA_DIVERSITY`、`PROD_CORRELATION`、`REGULAR_SUBMISSION` 的 `PENDING` 是 submission-only 状态，K deferred 到 L；simulation-resolved check 的 `PENDING/FAIL/ERROR` 仍阻止候选进入 L。
- live data-fields endpoint 不暴露 scalar unit、update cadence 和 missing sentinel 时不得伪造字段契约；batch 只允许 unary 或同 dataset、同量纲描述、显式定向的 pair 例外进入，并强制 `unitHandling=VERIFY`、`nanHandling=ON`，契约状态记录为 `TYPE_CONFIRMED_SERVER_UNIT_VERIFY_REQUIRED`。服务器确定性 unit 错误在 K 归入 family error，不能归为平台故障。

## Authentication recovery

- 自动续期必须覆盖 `204 / 401 / 429`，不能只覆盖 `401`；依据是 WQBRAIN 环境中 `wqb.WQBSession` 的 `expected` 判定。
- 所有 `WqbClient` 默认启用全局自动续期：每轮认证最多 3 次，业务请求最多额外重放 3 次，成功后更新 cookie。
- `sqlitesimu` 在 CoreClient 耗尽后独立补 5 次显式重登；额外层不递归进入 CoreClient 的重试循环。
- 两层均耗尽时，`204 / 401 / 429` 不得消耗 runtime 失败预算或写成 `PERMANENT_FAILURE`，任务保持 RUNNING 并延期恢复。
- 为保持与 `WQBSession` 一致，`POST /simulations` 返回 `204` 时也会重登并重放；认证接口自身禁止递归续期，登录仅接受 `201`。
- 旧 cookie loader 曾把同一个值同时挂到父域和 API host，导致续期后同一进程混发新旧 cookie；登录前必须清除旧 BRAIN cookie，旧扁平格式只加载一个 host cookie。
- alpha 等待和 simulation 轮询不得直接绕过 `WqbClient`，也不再保留局部 Basic Auth 重试。

## Retired template-group batch (2026-08-18)

- Research directory: `research_runs/run_20260818_121648_codex_template_group`
- Authoritative candidate file: `09_I_expression_candidates/expression_candidates.v2.json`
- Authoritative run: `run_d4be884a0b754a499bdcde55b3b193ff`
- Database: `10_J_sqlitesimu_batch/simulations.sqlite3`
- Manifest size: 2,400 unique expressions from 30 template families, 80 sampled without replacement per family.
- Initial worker PID `61608` was stopped while all 2,400 experiments were still `QUEUED`; schema v2 then backfilled both operational queues without changing experiment history.
- Worker PID `32276` was force-stopped after confirming the obsolete template set should not continue. The PID was later reused by an unrelated process and must not be treated as a worker identity.
- Run was durably cancelled with reason `obsolete_template_process_redesign`: after schema v3 terminology migration it is `1040 READY / 1350 CANCELLED / 10 SIMULATE_UNKNOWN`, simulation queue `10`, enrichment queue `0`.
- The 10 `SIMULATE_UNKNOWN` rows retain their simulation queue because their `POST /simulations` outcome cannot be proven; they must not be automatically simulated again or used for expansion eligibility.
- Existing 1040 READY results remain in SQLite for audit and descriptive analysis, but this cancelled run is ineligible for template expansion.
- Fixed settings: `CHN / D1 / TOP2000U`, decay 10, INDUSTRY neutralization, truncation 0.08, test period `P2Y0M0D`.
- This is the first density screen, not a 5,000-expression expansion. Only leading families may expand after terminal READY-rate, quality, and IS-PnL correlation analysis.
- Exact sign twins, commutative lag duplicates, reverse antisymmetric spreads, simple cross-sectional wrappers, mixed mechanisms, and unsupported operators are excluded before enqueue.
- Realized correlation cannot be inferred from expression shape; second-stage selection must cluster `simued_alpha_is_pnl` paths and avoid expanding multiple representatives of one high-correlation cluster.
- Diagnostic run `run_e3343135c9594b88b1aba4cc504652d9` is retired and must not be resumed. Its first six T18/T19 failures proved that `oth715_citics_1l` and `oth715_wind_3l` are `Unit[]`, not Group inputs. The v2 peer templates use platform built-in `industry` and `subindustry` values.

## Retired JPN template-v2 batch (2026-08-18)

- Workflow directory: `research_runs/workflow_batchsimu/run_20260818_164520_codex_template_v2`
- Authoritative run: `run_62c152032d6b4ec785886c05b0aff69b`
- Database: `10_J_sqlite_batch/simulations.sqlite3`
- Worker PID at handoff: `66616`; worker stdout/stderr are `NUL`, and liveness is read through `manage_worker.py status`.
- Target/settings: `JPN / D1 / TOP1600 / OTHER`, decay 6, INDUSTRY neutralization, truncation 0.08, `P2Y0M0D`.
- Manifest: 30 families x 80 = 2400; 176 fields; 2400 unique expressions, calculations and full simulation payloads.
- Manifest SHA-256: `c85c5ebee2434ef7183ed5ee4becf67bce99a5a39c8ff38cc019864f86ed4376`; consecutive deterministic generations matched.
- J enqueue accepted 2400 with zero reused candidates and zero duplicates. First worker status was `40 POLLING / 10 SIMULATING / 2350 QUEUED`.
- 用户确认模板结构质量不足后已永久取消：终态为 `510 READY / 1880 CANCELLED / 10 SIMULATE_UNKNOWN`，不得恢复 worker 或继续该 run。
- J20 was widened from one field pair to four matched polarity pairs; J23 was redesigned as four buy-conviction/attention-or-breadth ratios. No family has only one field tuple.
- Pre-simulation diversity requires at least two field tuples, 24 parameter signatures, both peer groups represented by at least 20 samples, and operator depth of at least five. These are only structural proxies; K still requires realized IS-PnL clustering.
- CHN/USA remain deprioritization advice rather than forbidden regions. This run selected JPN from current multiplier, tower and dataset evidence.
- The run is simulation-only. Agent leaves the loop after J and no Alpha submit action is authorized.

## Current BatchSimu campaign target (2026-08-18)

- 只运行独立 `workflow_batchsimu A-M`；不向其他研究流程 handoff。
- 新 batch 使用论坛证据设计真正不同的 AST/mechanism families，不用 wrapper、sign twin 或换字段伪装结构多样性。
- 活跃 run 不因中间质量停止，正常每 30--60 分钟紧凑监控；终态在同一 run 的 K/L/M 分析、终检和提交。
- M 成功计数必须满足 `wqb alpha submit` 输出 `ok = true && submit_code = 200`，并有提交后 Alpha GET 的平台状态证据。
- 未累计平台确认 4 条成功提交，就从本流程 A 创建下一独立 batch；弱结果只改变下一批模板设计，不结束 campaign。
- A-M contract 与 `template-report --minimum-ready-coverage` 已实现；2026-08-18 使用 WQBRAIN Python 的全量 `unittest` 为 `95/95` 通过，`git diff --check` 通过。

## Historical 5,000 batch (2026-08-17)

- Workflow directory: `local/workflows/batch_5000_20260817`
- Manifest size: 5000 unique expressions
- Canary run: `run_777b35a1ba394aea875051cbf55b31fe` (20/20 READY)
- Bulk run: `run_c758d733ca034b92b3b61945f01d9b72` (4980 expressions)
- Database: `local/workflows/batch_5000_20260817/simulations.sqlite3`
- Worker PID at launch: `2604`
- Heartbeat automation: `wqb-5000-batch-heartbeat` (15-minute interval)
- Worker output files: `bulk-worker.stdout.log`, `bulk-worker.stderr.log`

## Historical 5,000 batch design

- Universe: USA / D1 / TOP3000 / MATRIX
- 250 data fields, 20 low-complexity expressions per field
- Field filters include coverage/dateCoverage, usage thresholds, and dataset/category caps
- Expressions are direction-paired and use category-sensitive windows
- Fixed simulation settings: decay 4, SUBINDUSTRY neutralization, truncation 0.08, test period `P2Y0M0D`
