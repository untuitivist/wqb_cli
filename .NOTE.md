# Project Notes

## Confirmed intent

- 在 `wqb_cli` 仓库内实现可接入 workflow 的批量回测能力。
- 逻辑优先参考旧回测架构的 `send_simulate_request.py`、`get_simulate_result.py`、`get_is_pnl.py`；如果语义或行为发生变化，必须明确说明。
- agent 只负责批量生成或接收入库表达式并启动 `wqb sqlitesimu`，不参与逐条提交、轮询和结果抓取。
- 回测完成后，基于持久化的 `simued_alpha_is_pnl` 数据做批量分析和后续优化。
- 当前任务只做回测验证，不提交 alpha。
- 不使用 skill。

## Active batch

- Workflow directory: `local/workflows/batch_5000_20260817`
- Manifest size: 5000 unique expressions
- Canary run: `run_777b35a1ba394aea875051cbf55b31fe` (20/20 READY)
- Bulk run: `run_c758d733ca034b92b3b61945f01d9b72` (4980 expressions)
- Database: `local/workflows/batch_5000_20260817/simulations.sqlite3`
- Worker PID at launch: `2604`
- Heartbeat automation: `wqb-5000-batch-heartbeat` (15-minute interval)
- Worker output files: `bulk-worker.stdout.log`, `bulk-worker.stderr.log`

## Batch design

- Universe: USA / D1 / TOP3000 / MATRIX
- 250 data fields, 20 low-complexity expressions per field
- Field filters include coverage/dateCoverage, usage thresholds, and dataset/category caps
- Expressions are direction-paired and use category-sensitive windows
- Fixed simulation settings: decay 4, SUBINDUSTRY neutralization, truncation 0.08, test period `P2Y0M0D`
