# CLI 用法: `/achievements`

- Methods: `GET`
- Sources: `platform_frontend`
- Description: Achievements. / Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/achievements"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/achievements"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/achievements"
```

测试记录:

- Status: `tested`
- HTTP: `200 OK`
