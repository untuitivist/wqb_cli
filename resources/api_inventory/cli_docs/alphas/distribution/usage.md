# CLI 用法: `/alphas/distribution`

- Methods: `GET`
- Sources: `platform_frontend`
- Description: Alpha distribution aggregate. / Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/alphas/distribution"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/alphas/distribution"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/alphas/distribution"
```

测试记录:

- Status: `tested`
- HTTP: `404 Not Found`
