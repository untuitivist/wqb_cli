# CLI 用法: `/alphas/lists`

- Methods: `GET`
- Sources: `platform_frontend`
- Description: Alpha lists. / Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/alphas/lists"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/alphas/lists"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/alphas/lists"
```

测试记录:

- Status: `tested`
- HTTP: `404 Not Found`
