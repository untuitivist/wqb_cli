# CLI 用法: `/authentication/brainlabs`

- Methods: `GET`
- Sources: `platform_frontend`
- Description: Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/authentication/brainlabs"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/authentication/brainlabs"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/authentication/brainlabs"
```

测试记录:

- Status: `tested`
- HTTP: `302 Found`
