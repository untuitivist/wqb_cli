# CLI 用法: `/competition-levels`

- Methods: `GET`
- Sources: `platform_dynamic_capture, platform_frontend`
- Description: Competition levels. / Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/competition-levels"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/competition-levels"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/competition-levels"
```

测试记录:

- Status: `tested`
- HTTP: `200 OK`
