# CLI 用法: `/consultant-datasets`

- Methods: `GET`
- Sources: `platform_frontend`
- Description: Consultant datasets. / Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/consultant-datasets"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/consultant-datasets"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/consultant-datasets"
```

测试记录:

- Status: `tested`
- HTTP: `404 Not Found`
