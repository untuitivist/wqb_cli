# CLI 用法: `/consultant-information/consultant-dos-and-donts`

- Methods: `GET`
- Sources: `platform_frontend`
- Description: Consultant information article. / Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/consultant-information/consultant-dos-and-donts"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/consultant-information/consultant-dos-and-donts"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/consultant-information/consultant-dos-and-donts"
```

测试记录:

- Status: `tested`
- HTTP: `404 Not Found`
