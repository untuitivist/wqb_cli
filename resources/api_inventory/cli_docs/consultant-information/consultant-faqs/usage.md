# CLI 用法: `/consultant-information/consultant-faqs`

- Methods: `GET`
- Sources: `platform_frontend`
- Description: Consultant FAQ article. / Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/consultant-information/consultant-faqs"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/consultant-information/consultant-faqs"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/consultant-information/consultant-faqs"
```

测试记录:

- Status: `tested`
- HTTP: `404 Not Found`
