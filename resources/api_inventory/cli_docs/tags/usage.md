# CLI 用法: `/tags`

- Methods: `GET`
- Sources: `platform_frontend`
- Description: Tag list/search. / Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/tags"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/tags"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/tags"
```

测试记录:

- Status: `tested`
- HTTP: `200 OK`
