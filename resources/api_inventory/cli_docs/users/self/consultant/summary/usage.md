# CLI 用法: `/users/self/consultant/summary`

- Methods: `GET`
- Sources: `platform_dynamic_capture, platform_frontend`
- Description: Current consultant performance summary.

## 查看定义

```powershell
python -m wqb_cli api show "/users/self/consultant/summary"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/users/self/consultant/summary"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/users/self/consultant/summary"
```

测试记录:

- Status: `tested`
- HTTP: `200 OK`
