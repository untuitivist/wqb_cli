# CLI 用法: `/users/self/pyramid/alphas`

- Methods: `GET`
- Sources: `observed_platform`
- Description: Fallback pyramid alpha endpoint.

## 查看定义

```powershell
python -m wqb_cli api show "/users/self/pyramid/alphas"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/users/self/pyramid/alphas"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/users/self/pyramid/alphas"
```

测试记录:

- Status: `tested`
- HTTP: `404 Not Found`
