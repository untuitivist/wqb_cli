# CLI 用法: `/users/self/tutorial/steps`

- Methods: `GET`
- Sources: `platform_frontend`
- Description: Tutorial step state. / Discovered from platform frontend bundle.

## 查看定义

```powershell
python -m wqb_cli api show "/users/self/tutorial/steps"
```

## 调用方式

### `GET`

Command:

```powershell
python -m wqb_cli api call GET "/users/self/tutorial/steps"
```

实际执行:

```powershell
python -m wqb_cli api call GET "/users/self/tutorial/steps"
```

测试记录:

- Status: `tested`
- HTTP: `200 OK`
