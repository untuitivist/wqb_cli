# List SPC Prompt Submissions

```text
wqb competition spc submissions --limit 20 --offset 0
```

The response body is paginated and each result contains `id`, `name`, `prompt`, `sampleOutput`, `model`, `modelVersion`, `weight`, `updateFrequency`, `lastModified`, and `status`.
