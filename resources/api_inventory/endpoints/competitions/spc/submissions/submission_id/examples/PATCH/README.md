# Update an SPC Submission Weight

```text
wqb competition spc update-submission SUBMISSION_ID --method PATCH --input weight.json
```

This is a mutating request. The frontend currently sends the same weight-only shape shown in `weight.json`.
