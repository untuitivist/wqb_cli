# Request preview

Use `wqb api call /data-fields/{field_id}/visualize --method POST --dry-run` with the path variables in input.json. This is a request-shape example; mutating methods have not been executed and unresolved payload or query requirements are documented in endpoint.json.
