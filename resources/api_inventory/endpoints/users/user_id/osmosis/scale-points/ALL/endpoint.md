# /users/{user_id}/osmosis/scale-points/ALL

## API refresh 2026-09-22

Osmosis point-scaling progress. Retry-After means processing; 204 without Retry-After is a normal terminal response.

- Advertised methods: GET, HEAD.
- Live OPTIONS status: 405. Mutation methods were not executed.
- Account-specific option values are omitted; use `wqb api call` with OPTIONS for current metadata.
