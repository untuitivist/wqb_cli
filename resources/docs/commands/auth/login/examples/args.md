# Direct Argument Example

For login, direct argument mode is intentionally avoided because credentials must come from `.env` and request options are clearer through an input file.

Use the IO example for the real command.

