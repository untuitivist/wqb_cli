# File IO Example

`auth status` does not require input.
Use shell redirection or `Tee-Object` if a saved output file is needed:

```powershell
D:\_soft\Anaconda\envs\WQBRAIN\Scripts\wqb.exe auth status > wqb_cli\\docs\\commands\auth\status\outputs\status_output.json
```

