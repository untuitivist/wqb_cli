# File Output Example

Real command:

```powershell
D:\_soft\Anaconda\envs\WQBRAIN\Scripts\wqb.exe community search alpha --scope all --limit 3 --output "wqb_cli\docs\commands\community\search\outputs\search_alpha_output.json"
```

Output file:

```text
wqb_cli/docs/commands/community/search/outputs/search_alpha_output.json
```

This command has no JSON input file because the input is a search keyword and command options.
The persisted JSON output is the same object printed to stdout.

