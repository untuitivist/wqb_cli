# Notes

PYTHON multi-simulation is not supported by the platform.
Run PYTHON candidates one by one.

If the response is `401 Incorrect authentication credentials`, run `auth/login` and verify with `auth/status`.

If the simulation later returns `FAIL` or generic `ERROR` without code details, do not treat that as a proven economic failure.

