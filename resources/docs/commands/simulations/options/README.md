# simulations options

Fetch valid simulation fields, choices, and ranges.

Command:

```powershell
wqb sim options --output <output.json>
```

Generic API equivalent:

```powershell
wqb api call OPTIONS /simulations --input <input.json> --output <output.json>
```

Use this before changing universe, delay, neutralization, language, or other settings.

