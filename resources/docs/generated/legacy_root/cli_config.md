# WQB Config CLI

`wqb config` 是平台配置相关命令层�?
## `wqb config get`

获取平台配置�?
Raw API:

```text
GET /configuration
```

命令:

```powershell
wqb config get
```

验证记录:

## `wqb config competition-levels`

获取 competition levels�?
Raw API:

```text
GET /competition-levels
```

命令:

```powershell
wqb config competition-levels
```

验证记录:
